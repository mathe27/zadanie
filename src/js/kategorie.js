import { _kategorie } from './data/kategorie';
import  { _newsy } from './data/newsy';

const kategorie = () => {
    function init(_kategorie, _news) {
        const kategorie = _kategorie;
        const newsy = _newsy;
        console.log(self.newsy);
    }


};